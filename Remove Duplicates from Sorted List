1st


うーん。名前がちょっと冗長なような・・・
あと、新しいListNode()インスタンスを都度生成するのは速度的にどうなんだろうと。
``` python3 ``
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None:
            return head

        current_node = head

        duplication_removed_list_node = ListNode()
        duplication_removed_list_head = duplication_removed_list_node
        duplication_removed_list_node.val = current_node.val

        while current_node.next is not None:
            if current_node.val != current_node.next.val:
                duplication_removed_list_node.next = ListNode()
                duplication_removed_list_node.next.val = current_node.next.val

                duplication_removed_list_node = duplication_removed_list_node.next


            current_node = current_node.next

      
        return duplication_removed_list_head
``` python3 ``
